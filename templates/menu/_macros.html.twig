{% macro nav_link(item, isCurrent) %}
    {% import 'menu/_icons.html.twig' as icons %}
    {% set linkClass = isCurrent
        ? 'bg-gray-50 text-indigo-600 dark:bg-white/5 dark:text-white'
        : 'text-gray-700 hover:bg-gray-50 hover:text-indigo-600 dark:text-gray-400 dark:hover:bg-white/5 dark:hover:text-white' %}
    {% set iconClass = isCurrent
        ? 'text-indigo-600 dark:text-white'
        : 'text-gray-400 group-hover:text-indigo-600 dark:group-hover:text-white' %}
    <a href="{{ item.uri }}" class="group flex gap-x-3 rounded-md p-2 text-sm/6 font-semibold {{ linkClass }}">
        {{ icons.icon(item.extra('icon'), 'size-6 shrink-0 ' ~ iconClass) }}
        {{ item.label }}
    </a>
{% endmacro %}
