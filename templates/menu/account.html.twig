{% extends 'knp_menu_base.html.twig' %}

{% block root %}
    {% for child in item.children %}
        <a href="{{ child.uri }}" class="block px-3 py-1 text-sm/6 text-gray-900 focus:bg-gray-50 focus:outline-hidden dark:text-white dark:focus:bg-gray-700">{{ child.label }}</a>
    {% endfor %}
{% endblock %}
